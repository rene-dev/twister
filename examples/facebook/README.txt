This example web application displays a user's Facebook news feed.  The
application uses OAuth2 to authenticate with Facebook.

To run this example:

1. Copy settings.json.example to settings.json.
2. Edit settings.json to include your Facebook applicaiton identifier 
   and secret. You can get an identifier and secret by registring
   an application at http://developers.facebook.com/setup/.
3. make run
